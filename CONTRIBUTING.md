# Guía de Contribución

1. Clona el repositorio y ejecuta `npm install`.
2. Crea una rama descriptiva (`git checkout -b feat/nueva-pantalla`).
3. Sigue la convención de código enforced por Prettier y ESLint.
4. Abre un Pull Request utilizando la plantilla predefinida.
